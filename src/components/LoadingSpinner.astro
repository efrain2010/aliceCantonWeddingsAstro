---
interface Props {
  className?: string;
}

const { className = "h-12 w-12 border-white/25 border-t-white" } =
  Astro.props;
---

<div
  aria-hidden="true"
  class={`animate-spin rounded-full border-4 ${className}`}
>
</div>
